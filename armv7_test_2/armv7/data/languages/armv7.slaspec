# Auto-generated SLEIGH specification for ARM Cortex-M
# Generated by rosetta — do not hand-edit unless correcting P-code.

define endian=little;
define alignment=4;

# Address spaces
define space ram      type=ram_space      size=4  wordsize=1 default;
define space register type=register_space size=4;

# Register file
# Registers are laid out sequentially: R0, R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, SP, LR, PC
define register offset=0x00 size=4 [
    R0
    R1
    R2
    R3
    R4
    R5
    R6
    R7
    R8
    R9
    R10
    R11
    R12
    SP
    LR
    PC
];

# Register aliases: SP, LR, PC have aliases — omitted to avoid duplicate-offset errors in SLEIGH.

# Tokens (instruction bit fields)
define token instr16(16)
    op16stub = (0,15)
;

define token instr32(32)
    opcode32 = (011001,011001)
    Rd32 = (8,11)
    Rn32 = (4,7)
    Rm32 = (0,3)
    funct332 = (000,000)
    rs232 = (5,7)
    rd32 = (8,11)
    rs132 = (16,20)
    funct732 = (25,31)
    shifter_operand32 = (8,11)
    Rs32 = (16,19)
    shift_type32 = (7,7)
    imm2432 = (0,25)
    res132 = (26,31)
    imm1932 = (5,24)
    link32 = (4,4)
    imm1232 = (20,31)
    S32 = (0,0)
    imm832 = (0,7)
    imm11_832 = (4,7)
    imm4_032 = (8,11)
    cond32 = (12,15)
    label32 = (0,24)
    cbreak32 = (0,2)
    inst32 = (0111011,0111011)
    rs32 = (5,7)
    rt32 = (2,4)
    shamt32 = (6,11)
    csr32 = (24,31)
    opc132 = (5,6)
    imm1632 = (0,15)
    r132 = (11,8)
    base_reg32 = (8,11)
    index_reg32 = (4,7)
    shift_amount32 = (0,3)
    instruction_type32 = (4,5)
    register_n32 = (0,3)
    register_d32 = (16,19)
    immediate_832 = (24,31)
    immediate32 = (0,7)
    register_Rd32 = (12,15)
    register_Rn32 = (8,11)
    ldst32 = (7,7)
    size32 = (8,8)
    rn32 = (15,12)
    opcode132 = (5,6)
    shifter_immediate32 = (0,4)
    opcode232 = (28,31)
    fn32 = (5,5)
    Wd32 = (16,19)
    Wn32 = (24,27)
    Wm32 = (20,23)
    Wl32 = (19,22)
    opc232 = (2,3)
    Ra32 = (12,15)
    imm32 = (0,24)
    instruction32 = (2,0)
    rm32 = (0,3)
    Rd_lo32 = (16,19)
    Rn_lo32 = (20,23)
    Rm_lo32 = (24,27)
    Rd_hi32 = (28,31)
    s32 = (4,4)
    sat_type32 = (4,4)
    funct32 = (20,31)
    N32 = (31,31)
    Op132 = (24,26)
    RdL32 = (20,23)
    Op232 = (9,11)
    rd_lo32 = (10,10)
    rn_lo32 = (12,12)
    rm_lo32 = (14,14)
    res032 = (15,31)
    imm532 = (20,24)
    immed732 = (8,13)
    Rt32 = (8,11)
    n32 = (28,29)
    x32 = (26,27)
    immed832 = (24,31)
    Instruction32 = (28,31)
    VAddr32 = (0,27)
    instr32 = (5,0)
    immed1232 = (12,23)
    Rn_hi32 = (16,19)
    op32stub = (0,31)
;


# Instruction constructors
# ADD: Adds the contents of Rn and Rm, and stores the result in Rd. In the...
:ADD is op32stub=0
{
    # rd = rn + rm;
    local tmp:4 = 0;
}

# AND: Logical AND of registers Rn and Rm, result stored in Rd.
:AND is op32stub=1
{
    # rd = ra & rb;
    local tmp:4 = 0;
}

# ASR: Arithmetic shift right of Rm by the amount specified in shifter_operand,...
:ASR is op32stub=2
{
    # rd = asr(rm, ra);
    local tmp:4 = 0;
}

# B: Branch to the address specified by addr24. The processor performs a branch...
:B is op32stub=3
{
    # "goto [addr24];"
    local tmp:4 = 0;
}

# BL: Branch to the target address specified by label, and link register (LR) is...
:BL is op32stub=4
{
    # "lr = ra + 4; goto [*ram]:32 ra;"
    local tmp:4 = 0;
}

# BLX: Branch to address in R14 (lr) with link, effectively calling a subroutine.
:BLX is op32stub=5
{
    # goto [*ram]:32 ra + 4;
    local tmp:4 = 0;
}

# BREAK: Causes a breakpoint exception, where the value of imm8 is passed as an...
:BREAK is op32stub=6
{
    # exception tmp = imm8;
    local tmp:4 = 0;
}

# BX: Branch to the address in the link register (LR). The program counter is set...
:BX is op32stub=7
{
    # "goto [lr];"
    local tmp:4 = 0;
}

# CBNZ: Branch to label if Rn is not zero.
:CBNZ is op32stub=8
{
    # goto ![rn];
    local tmp:4 = 0;
}

# CBREAK: Causes the processor to break out of a debug monitor or simulator environment.
:CBREAK is op32stub=9
{
    # call [debug_monitor];
    local tmp:4 = 0;
}

# CBZ: Branch to label if Rd is zero.
:CBZ is op32stub=10
{
    # gotoifzero [rd];
    local tmp:4 = 0;
}

# CLREXCPTN: Clears the exception bit in the CPSR (Current Program Status Register).
:CLREXCPTN is op32stub=11
{
    # rd = rd & ~0x1;
    local tmp:4 = 0;
}

# CLZ: Counts the leading zeros in the value of Rm and stores the result in Rd. If...
:CLZ is op32stub=12
{
    # rd = clz(rm);
    local tmp:4 = 0;
}

# CMP: Subtracts the value of Rm (or immediate imm8) from the value in the...
:CMP is op32stub=13
{
    # "tmp = ra - rm; ra = tmp;"
    local tmp:4 = 0;
}

# CSRRC: Read the value of a CSR, complement its bits specified by CSRn, and write...
:CSRRC is op32stub=14
{
    # rd = csrccomplement(rs, ra);
    local tmp:4 = 0;
}

# CSRRS: Reads the value from the specified CSR and performs a bitwise AND with the...
:CSRRS is op32stub=15
{
    # "rd = (csr[rs1]; tmp = rd; rd = tmp & ra; x1 = tmp;);;"
    local tmp:4 = 0;
}

# CSRRW: Read a value from the specified CSR, store it in rd, and write the new value...
:CSRRW is op32stub=16
{
    # rd = ra; ra = rd;
    local tmp:4 = 0;
}

# CSRRWI: Read from a CSR with write enable, writing the result to rd. The instruction...
:CSRRWI is op32stub=17
{
    # rd = csrwimm[11:0](rs1);
    local tmp:4 = 0;
}

# CVT: Converts between floating-point and integer formats using the specified...
:CVT is op32stub=18
{
    # rd = cvt(rs, tmp);
    local tmp:4 = 0;
}

# CYCLE: Performs a cycle count increment by the value specified in the immediate field.
:CYCLE is op32stub=19
{
    # tmp = rs + 1; ra = tmp;
    local tmp:4 = 0;
}

# CYCLEH: Increments the value of the CPSR's H (saturation) bit by the immediate...
:CYCLEH is op32stub=20
{
    # rd = clamp((rs + 1), 0, 1);
    local tmp:4 = 0;
}

# DELEGATE: This instruction is used to delegate a capability or privilege from one...
:DELEGATE is op32stub=21
{
    # "rd = delegate[rs];"
    local tmp:4 = 0;
}

# FENCE: The FENCE instruction ensures that all memory accesses before the...
:FENCE is op32stub=22
{
    # fence [iq];
    local tmp:4 = 0;
}

# FENCE.I: The FENCE.I instruction ensures that all memory accesses before the...
:FENCE.I is op32stub=23
{
    # fence;
    local tmp:4 = 0;
}

# HYPERCALL: Performs a hypercall to the operating system with the specified register and...
:HYPERCALL is op32stub=24
{
    # rd = call [rs + tmp]:32;
    local tmp:4 = 0;
}

# LD: Load the contents of the memory location specified by the base register (Rn)...
:LD is op32stub=25
{
    # rd = *[ram]:4 rn + rm;
    local tmp:4 = 0;
}

# LDB: Load byte from memory into Rd. The effective address is calculated as Rn + imm8.
:LDB is op32stub=26
{
    # rd = *[ram]:1 rn + imm8;
    local tmp:4 = 0;
}

# LDH: Load half-word from memory into Rd. The address is calculated as Rn + imm8,...
:LDH is op32stub=27
{
    # rd = *((ram):2 (rn + imm8));
    local tmp:4 = 0;
}

# LDQ: Load a quadword (64 bits) from the address calculated as Rn + imm12 into register Rd.
:LDQ is op32stub=28
{
    # rd = *[ram]:8 rn + imm12;
    local tmp:4 = 0;
}

# LSL: Logical shift left the value in Rm by imm5 positions, storing the result in Rd.
:LSL is op32stub=29
{
    # rd = rdshl5(rs, ra);
    local tmp:4 = 0;
}

# LSR: Logical shift right the contents of Rm by imm5 positions, storing the result...
:LSR is op32stub=30
{
    # rd = rdzero(rm >> 5);
    local tmp:4 = 0;
}

# MLAL: Multiplies the contents of registers Rn and Rs, then adds the result to the...
:MLAL is op32stub=31
{
    # rd = (ra * rb) + rm;
    local tmp:4 = 0;
}

# MLALH: Multiplies the high halves of registers Wn and Wm, adds the result to the...
:MLALH is op32stub=32
{
    # rd = (tmp = ra * rb, (tmp + rc) >> 32);
    local tmp:4 = 0;
}

# MLALS: Multiplies the contents of registers Rn and Rm, adds the result to the...
:MLALS is op32stub=33
{
    # rd = (ra << 32) + (ra & 0xFFFFFFFF) + (rs * rt); ra = ra + (rs * rt >> 32);
    local tmp:4 = 0;
}

# MLS: Multiplies the contents of Rm by the contents of Rn and subtracts the result...
:MLS is op32stub=34
{
    # rd = (rd - (rm * rn)); tmp = rd;
    local tmp:4 = 0;
}

# MLSHP: Performs a multiply and subtract operation with the high part of the result....
:MLSHP is op32stub=35
{
    # rd = ext_shiftr(ra, mulshiftr(rb, ra), 32) - ra;
    local tmp:4 = 0;
}

# MLSL: Multiplies the contents of Rn and Rp, then subtracts the result from Rd. The...
:MLSL is op32stub=36
{
    # rd = (rs * rt) - rd;
    local tmp:4 = 0;
}

# MOV: Moves the value from register Rm or immediate imm32 into register Rd.
:MOV is op32stub=37
{
    # rd = rm;
    local tmp:4 = 0;
}

# MOVPC: Moves the value of an immediate 24-bit value into the program counter,...
:MOVPC is op32stub=38
{
    # pc = tmp; tmp = imm24; goto [tmp];
    local tmp:4 = 0;
}

# MRET: Pops the address from the exception return stack and transfers control to...
:MRET is op32stub=39
{
    # "goto [*stack_ptr + 4 * ra];"
    local tmp:4 = 0;
}

# MUL: Multiplies the contents of Rn and Rm, storing the result in Rd. The...
:MUL is op32stub=40
{
    # rd = ra * rb;
    local tmp:4 = 0;
}

# NEG: Negates the value in register Rs1 and stores the result in Rd.
:NEG is op32stub=41
{
    # rd = -rs1;
    local tmp:4 = 0;
}

# NOT: Inverts the bits of the value in register Rn and stores the result in register Rd.
:NOT is op32stub=42
{
    # rd = ~rn;
    local tmp:4 = 0;
}

# ORR: Performs a bitwise OR operation between the contents of Rn and Rm, storing...
:ORR is op32stub=43
{
    # rd = ra | (rb ? rb : ra);
    local tmp:4 = 0;
}

# PACK: Pack the lower 32 bits of Rm and Rn into Rd, with the lower 16 bits in Rd_lo...
:PACK is op32stub=44
{
    # rd_lo = rm & 0xFFFF; rd_hi = (rn >> 16) & 0xFFFF;
    local tmp:4 = 0;
}

# REV16: Reverses the order of 8-bit halfwords in the 16-bit value in register Rm and...
:REV16 is op32stub=45
{
    # rd = __rev16__(rs);
    local tmp:4 = 0;
}

# REV24: Reverses the order of 24 bits in the value of Rm and stores the result in Rd.
:REV24 is op32stub=46
{
    # rd = __rev24__(rs);
    local tmp:4 = 0;
}

# REV8: Reverses the byte order of the 32-bit value in register Rm and stores the...
:REV8 is op32stub=47
{
    # rd = bswap_8(rm);
    local tmp:4 = 0;
}

# ROR: Rotates the value in Rn right by imm5 bits and stores the result in Rd.
:ROR is op32stub=48
{
    # rd = ror(rd = rn, imm5);
    local tmp:4 = 0;
}

# SAT: Saturating addition of register and immediate, storing result in rd. The...
:SAT is op32stub=49
{
    # rd = sat[cond](ra + rs);
    local tmp:4 = 0;
}

# SDIV: Signed divide the value in Rs by the value in Rm, and store the result in Rd.
:SDIV is op32stub=50
{
    # rd = sdiv(rm, rs);
    local tmp:4 = 0;
}

# SDIVAW: Undefined instruction, no operation performed.
:SDIVAW is op32stub=51
{
    # nop;
    local tmp:4 = 0;
}

# SDIVW: Signed divide the value in Rm by the value in Rs, with the result stored in...
:SDIVW is op32stub=52
{
    # rd = sdiv cc, [ra], [rb];
    local tmp:4 = 0;
}

# SFENCE.VMA: Synchronizes the virtual memory access of the current thread with respect to...
:SFENCE.VMA is op32stub=53
{
    # pseudocode: "sfence [ra];"
    local tmp:4 = 0;
}

# SLLI: Shifts the value in register Rn left by imm5 bits and stores the result in Rd.
:SLLI is op32stub=54
{
    # rd = ra << (rs & 0x1f);
    local tmp:4 = 0;
}

# SLLIW: Shifts the value in register rs left by imm5 bits and stores the result in...
:SLLIW is op32stub=55
{
    # rd = (rs << (imm5 & 0x1F));
    local tmp:4 = 0;
}

# SMULL: Multiplies the contents of Rm and Rn, sign-extends the result to 64 bits,...
:SMULL is op32stub=56
{
    # rd = (int64_t)(rs1 * rs2) >> 32; tmp = (int64_t)(rs1 * rs2);
    local tmp:4 = 0;
}

# SMULLH: Performs a signed multiplication of Rn and Rm, storing the lower 32 bits in...
:SMULLH is op32stub=57
{
    # rdl = rdh = (rs * rt) >> 32;
    local tmp:4 = 0;
}

# SMULLS: Performs a signed multiplication of Rn and Rm, storing the lower 32 bits in...
:SMULLS is op32stub=58
{
    # rd_lo = (rs * rt):64 & 0xFFFFFFFF;
    local tmp:4 = 0;
}

# SQADD: Subtracts the value in register Sm from the value in register Rn and stores...
:SQADD is op32stub=59
{
    # rd = rs - ra;
    local tmp:4 = 0;
}

# SQDADD: Subtracts the double-precision floating-point value in Qm from the value in...
:SQDADD is op32stub=60
{
    # rd = vfp_sqdadd(qd, qn, qm);
    local tmp:4 = 0;
}

# SQDSUB: Subtracts the signed value of register Rs from the signed value of register...
:SQDSUB is op32stub=61
{
    # rd = (rs < 0) ? -(~rs & 0xFFFFFFFF) - rn + 1 : rs - rn;
    local tmp:4 = 0;
}

# SQSUB: Subtracts the value in register Sm from the value in register Rn,...
:SQSUB is op32stub=62
{
    # sd = sign_ext[(rn - sm):32];
    local tmp:4 = 0;
}

# SRAI: Shifts the value in register Rn right arithmetically by imm5 bits and stores...
:SRAI is op32stub=63
{
    # rd = srar(ra, imm5);
    local tmp:4 = 0;
}

# SRARI: Shifts the value in Rm right by imm5 bits and stores the result in Rd.
:SRARI is op32stub=64
{
    # rd = (rs >> imm5);
    local tmp:4 = 0;
}

# SRLI: Shifts the value in register Rs right logical by imm5 bits and stores the...
:SRLI is op32stub=65
{
    # rt = rs >> imm5;
    local tmp:4 = 0;
}

# SRLIW: Shifts the value in register Rn right logical by immediate imm5 bits and...
:SRLIW is op32stub=66
{
    # rd = (rs >> imm5);
    local tmp:4 = 0;
}

# ST: Store the value in register Rt to the memory address calculated as (Rn + Rm).
:ST is op32stub=67
{
    # rd = *[ram]:4 (rs + ra);
    local tmp:4 = 0;
}

# STB: Stores the value of register Rt into memory at the address calculated by...
:STB is op32stub=68
{
    # "*[ram]:1 = rt + rn + imm8;"
    local tmp:4 = 0;
}

# STH: Stores the 16-bit sign-extended value of Rt into memory starting at address...
:STH is op32stub=69
{
    # "*[ram]:2 tmp = ext8:16 rt; *[ram + ra + imm]:2 tmp;"
    local tmp:4 = 0;
}

# SUB: Subtracts the value in Rm from the value in Rn and stores the result in Rd....
:SUB is op32stub=70
{
    # rd = rn - rm;
    local tmp:4 = 0;
}

# SYSCALL: Transitions to the kernel mode and invokes a system call with the current...
:SYSCALL is op32stub=71
{
    # syscall;
    local tmp:4 = 0;
}

# TGEI: If Rn > Rd, then branch to the address given by imm12 (sign-extended)....
:TGEI is op32stub=72
{
    # "goto [(imm12:16) + ra];"
    local tmp:4 = 0;
}

# TGHI: Takes the high 8 bits of an immediate value and stores it in Rd, with Rn...
:TGHI is op32stub=73
{
    # rd = (imm >> 24) & 0xFF;
    local tmp:4 = 0;
}

# TLBWI: Write-in instruction to update the Translation Lookaside Buffer (TLB) with a...
:TLBWI is op32stub=74
{
    # tmp = ra; *[ram]:8 tmp = 0x1234567890abcded; goto [ra + 4];
    local tmp:4 = 0;
}

# TLBWR: Write a value to the Translation Lookaside Buffer (TLB). The exact address...
:TLBWR is op32stub=75
{
    # "goto [tlb_write];"
    local tmp:4 = 0;
}

# TWAI: This instruction is used for handling TWAI (Time-Triggered Automotive...
:TWAI is op32stub=76
{
    # "rd = (rs | 1) & ~2;"
    local tmp:4 = 0;
}

# TWRI: Truncate and write the value of register Rs to memory, with the immediate...
:TWRI is op32stub=77
{
    # "*[ram]:4 tmp = rs; tmp = tmp & 0x0fff; *[ram + rt*4]:4 = tmp;"
    local tmp:4 = 0;
}

# UDIV: Unsigned divide Rd = Rn / Rs, with remainder discarded.
:UDIV is op32stub=78
{
    # rd = udiv(rn, rs);
    local tmp:4 = 0;
}

# UDIVAW: Unsigned divide word, with remainder. This instruction performs an unsigned...
:UDIVAW is op32stub=79
{
    # rd = udivw(rn, rm); r0 = unrmdr(rn, rm);
    local tmp:4 = 0;
}

# UDIVW: Unsigned divide of the value in register Rn by the value in register Rs,...
:UDIVW is op32stub=80
{
    # rd = udivw(rs, ra); cc = rd;
    local tmp:4 = 0;
}

# UMULL: Performs an unsigned multiply of Rn and Rm, storing the lower 32 bits in...
:UMULL is op32stub=81
{
    # "rd_lo = rd_hi = [ra * rb]:64; cc = cc_status;"
    local tmp:4 = 0;
}

# UMULLH: Performs an unsigned multiply of Rn and Rm, storing the higher half of the...
:UMULLH is op32stub=82
{
    # rd_lo = (ra * rb) >> 32;
    local tmp:4 = 0;
}

# UMULLS: Performs an unsigned multiply of Rn and Rm, storing the lower 32 bits in...
:UMULLS is op32stub=83
{
    # rd_lo = (uint32_t)rs * (uint32_t)rt;
    local tmp:4 = 0;
}

# UQDADD: Unsigned saturating double-width add of Rn and Rm, result stored in Rd.
:UQDADD is op32stub=84
{
    # rd = uqadd64(ra, rb);
    local tmp:4 = 0;
}

# UQDSUB: Subtracts the value of Rm (sign-extended to 32 bits) from the value in Rn,...
:UQDSUB is op32stub=85
{
    # rd = sign_ext((int32_t)(*(ram):32 rs) - (int32_t)(ra));
    local tmp:4 = 0;
}

# XOR: Performs a bitwise exclusive OR operation on the values in registers Rn and...
:XOR is op32stub=86
{
    # rd = ra ^ rb;
    local tmp:4 = 0;
}

