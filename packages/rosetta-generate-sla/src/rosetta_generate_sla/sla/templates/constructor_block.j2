{% for instr in instructions %}
# {{ instr.mnemonic }}: {{ instr.semantics | truncate(80) }}
:{{ instr.mnemonic }} is op{{ instr.encoding_bits }}stub={{ stub_base + loop.index0 }}
{
    {{ instr.pcode_hint | render_pcode }}
}

{% endfor %}
